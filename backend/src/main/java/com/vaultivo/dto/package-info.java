/**
 * Request/response DTOs exposed by controllers. Never expose JPA entities
 * directly over the API — map to/from DTOs (MapStruct) at the controller
 * or service boundary.
 */
package com.vaultivo.dto;
