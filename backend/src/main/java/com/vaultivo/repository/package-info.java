/**
 * Spring Data JPA repositories — one per entity in {@link com.vaultivo.model}.
 * Keep custom queries here (via @Query or derived method names); no business
 * logic belongs in this layer.
 */
package com.vaultivo.repository;
